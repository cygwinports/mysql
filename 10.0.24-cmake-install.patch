--- origsrc/mariadb-10.0.24/cmake/install_macros.cmake	2016-02-17 14:59:54.000000000 -0600
+++ src/mariadb-10.0.24/cmake/install_macros.cmake	2016-03-03 13:10:50.992922500 -0600
@@ -286,7 +286,13 @@ FUNCTION(MYSQL_INSTALL_TARGETS)
     ENDIF()
   ENDFOREACH()
 
-  INSTALL(TARGETS ${TARGETS} DESTINATION ${ARG_DESTINATION} ${COMP})
+  IF(${ARG_DESTINATION} STREQUAL "${INSTALL_LIBDIR}")
+    INSTALL(TARGETS ${TARGETS} LIBRARY DESTINATION ${ARG_DESTINATION}
+                               RUNTIME DESTINATION bin
+                               ARCHIVE DESTINATION ${ARG_DESTINATION} ${COMP})
+  ELSE()
+    INSTALL(TARGETS ${TARGETS} DESTINATION ${ARG_DESTINATION} ${COMP})
+  ENDIF()
   INSTALL_DEBUG_SYMBOLS(${TARGETS} ${COMP} INSTALL_LOCATION ${ARG_DESTINATION})
 
 ENDFUNCTION()
